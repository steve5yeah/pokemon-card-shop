# 포켓몬 카드샵 🔴⚪ — 작업 규칙

개발을 배우는 중인 **비개발자**와 함께 만드는 연습용 프로젝트입니다.
Claude는 아래 규칙을 항상 지켜 주세요.

---

## 1. 말투 · 설명 방식

- **모든 대화는 한국어로.**
- 개발 용어가 나오면 **꼭 쉬운 말로 풀어서** 설명합니다.
  - 예) "마이그레이션" → "데이터베이스 구조를 바꾸는 작업(표에 칸 추가하기 같은 것)"
  - 예) "환경 변수" → "비밀번호처럼 코드에 직접 적으면 안 되는 값을 따로 적어 두는 메모장(`.env.local` 파일)"
- 파일 경로나 명령어는 그대로 보여 주되, **이게 뭘 하는 건지 한 줄 설명**을 붙입니다.
- 어려운 영어 약어(RLS, SSR, CI 등)는 처음 쓸 때 괄호로 뜻을 적어 줍니다.

## 2. 진행 상황 자주 알리기

- 작업 시작 전: **"지금부터 ○○를 만들겠습니다"** 라고 먼저 말합니다.
- 작업 중간중간: **"지금 ○○ 파일을 고치는 중입니다"** 처럼 짧게 현재 위치를 알립니다.
- 작업 후: **무엇이 바뀌었는지, 화면에서 뭐가 달라 보이는지** 를 정리해서 알려 줍니다.
- 조용히 오래 작업하지 않습니다.

## 3. 단계적으로만 진행

- **사용자가 시킨 것만 합니다.** 시키지 않은 기능은 미리 만들지 않습니다.
- "다음 단계는 ○○인데 지금 해 드릴까요?" 하고 **물어보고 기다립니다.**
- 한 단계 끝 → 확인 → 다음 단계.
- 개선하고 싶은 게 보이면 **바로 고치지 말고 먼저 제안**합니다.

## 4. 할 수 있는 건 최대한 Claude가

- 파일 만들기 · 고치기, 패키지 설치, 개발 서버 실행, 데이터베이스 표 만들기(Supabase MCP), 화면 확인, git 커밋 → **전부 Claude가 직접** 합니다.
- **사람만 할 수 있는 일**만 부탁합니다. 이때는 ① 어느 사이트 ② 어느 메뉴 ③ 무엇을 입력/복사 ④ 결과를 어떻게 알려줄지 4단계로 안내합니다.
- 사람이 해야 하는 일 예시: Supabase 대시보드 설정 변경, Vercel 계정 연결, GitHub 저장소 만들기, 결제·비밀번호 입력.

## 5. 다 만들면 바로 열어서 보여 주기

- 코드를 고친 뒤에는 **반드시 브라우저로 직접 열어서 동작을 확인**합니다.
  - `preview_start` 로 `poke-dev` 서버를 켜고 (`.claude/launch.json`에 설정되어 있음)
  - 화면을 읽거나 스크린샷을 찍어 **"이렇게 나옵니다"** 하고 보여 줍니다.
- 사용자에게 "직접 확인해 보세요"라고 떠넘기지 않습니다.
- 에러가 있으면 **브라우저 콘솔 · 서버 로그까지 확인**하고 고친 뒤 다시 보여 줍니다.

---

## 프로젝트 정보

| 항목 | 내용 |
| --- | --- |
| 만드는 것 | 포켓몬 트레이딩 카드 쇼핑몰 (장바구니 · 결제 · 주문내역 · 관리자) |
| 화면 만드는 도구 | **Next.js 15** (App Router) + React 19 + TypeScript |
| 디자인 | **Tailwind CSS v4** — 색은 `src/app/globals.css`의 `@theme`에 정의 (`poke-*` 빨강, `volt-*` 노랑, `ink-*` 남색) |
| 아이콘 | `src/components/Icons.tsx` 의 SVG — **이모지를 쓰지 않습니다** (기기마다 모양이 달라짐) |
| 회원·데이터 보관 | **Supabase** — 프로젝트 `pokemon-card-shop` / ref `ezpigwtshysvtmnaptwv` (ap-northeast-2) |
| 결제 | **토스페이먼츠 결제위젯 v2** (`@tosspayments/tosspayments-sdk`) — 지금은 문서용 테스트 키 |
| 인터넷 공개(배포) | **Vercel** (7단계에서 연결) |
| 실행 | `npm run dev` → http://localhost:3000 |

### 디자인 규칙 🎨

포켓몬 공식 사이트(두꺼운 검정 아웃라인 · 모서리를 비스듬히 자른 각진 블록)와
**진짜 포켓몬 카드의 금색 테두리**를 참고해 만들었습니다. 새 화면을 만들 때도 이 규칙을 따릅니다.

| 클래스 | 쓰는 곳 |
| --- | --- |
| `.tcg` + `.tcg-in` | 카드를 **진짜 트레이딩 카드처럼** 보이게 하는 금색 테두리 두 겹 |
| `.holo` | 아트레어 이상 등급의 무지개 반짝임 (그림칸에 붙임) |
| `.thumb` | 목록에 들어가는 작은 카드 그림 (장바구니 · 주문내역 · 관리자) |
| `.night` / `.night-stars` | 어두운 쇼케이스 배경 (첫 화면 · 푸터) |
| `.ribbon` | 제목 위에 붙는 검정 라벨 (`CART`, `ORDER` 처럼 영어 한 단어) |
| `.cut` / `.cut-sm` / `.cut-both` | 모서리를 비스듬히 자르는 모양 |
| `.btn-primary` / `.btn-volt` / `.btn-outline` / `.btn-ghost` | 버튼 4종 |
| `.card` / `.card-dark` / `.card-night` | 상자 3종 (`card-dark` 는 합계·금액처럼 결론을 담는 칸) |

- 테두리는 **2px `ink-900`**, 그림자는 흐림 없는 **`3px 3px 0`** (스티커처럼 톡 튀어나와 보이게).
- ⚠️ `.cut` 계열은 `clip-path` 라서 **테두리까지 같이 잘립니다.** 배경이 꽉 찬 요소에만 씁니다.
- ⚠️ 아주 좁은 화면(320px)에서 줄이 넘치지 않는지 꼭 확인합니다.
  `document.documentElement.scrollWidth - clientWidth` 가 **0** 이어야 합니다.

### Supabase 주의사항 ⚠️

- 쇼핑몰은 **전용 Supabase 프로젝트**(`pokemon-card-shop`, `ezpigwtshysvtmnaptwv`)를 씁니다.
- 가계부 · 고구마마켓 · 세무사 홈페이지가 쓰는 프로젝트(`gagyebu`, `fpffmmbuiomcfdrjxnck`)와 **다른 프로젝트**입니다. **절대 혼동하지 마세요.**
  MCP 도구를 쓸 때 `project_id`가 `ezpigwtshysvtmnaptwv` 인지 매번 확인합니다.
- 쇼핑몰 전용 프로젝트라 표 이름에 접두사를 붙이지 않습니다 (`products`, `orders`처럼 짧게).

### 돈을 다루는 규칙 (가장 중요) 💰

- **금액은 브라우저가 절대 정하지 못하게 합니다.** 폼으로 금액을 받지 않고, 항상 데이터베이스의 값으로 다시 계산합니다.
- `orders` 표에는 **사용자용 INSERT · UPDATE 정책을 만들지 않습니다.** 주문 생성과 결제 확정은 Postgres 함수(`create_order`, `complete_order`)만 할 수 있습니다.
- `TOSS_SECRET_KEY` 는 **절대** `next.config.ts` 의 `env` 항목에 넣지 않습니다. 넣으면 브라우저에 그대로 노출됩니다.
- 시크릿 키를 쓰는 파일은 맨 윗줄에 `import "server-only";` 를 넣습니다.

### 폴더 구조

```
src/
├─ middleware.ts          로그인 상태 유지 + /cart, /checkout, /orders, /mypage, /admin 보호
├─ lib/
│  ├─ supabase/           Supabase 연결 코드 (client / server / middleware)
│  └─ payments/           토스 결제 (5단계)
├─ components/            여러 화면에서 같이 쓰는 조각들 (헤더 · 몬스터볼 로고 · 버튼)
└─ app/
   ├─ layout.tsx          모든 화면 공통 틀 (헤더 · 푸터)
   ├─ globals.css         포켓몬 색 팔레트 + 공용 클래스(.btn / .field / .card / .chip)
   ├─ page.tsx            홈
   ├─ auth/               가입 · 로그인 · 로그아웃 처리
   ├─ login/ signup/      로그인 · 가입 화면
   ├─ products/           카드 목록 · 상세 (2~3단계)
   ├─ cart/               장바구니 (4단계)
   ├─ checkout/           주문서 · 결제위젯 (5단계)
   ├─ api/payments/       토스가 결제 후 돌아오는 자리 (5단계)
   ├─ orders/             주문내역 (6단계)
   ├─ mypage/             내 정보 (6단계)
   └─ admin/              관리자 상품 · 주문 관리 (6단계)
```

### 진행 단계

- [x] **1단계** — 프로젝트 뼈대 · 포켓몬 디자인 · 회원가입 / 로그인 / 로그아웃
- [x] **2단계** — 카드 목록 · 상세 (샘플 카드 36장 자동 등록)
- [x] **3단계** — 검색 · 필터(희귀도 · 타입 · 확장팩 · 가격대) · 정렬
- [x] **4단계** — 장바구니
- [x] **5단계** — 주문 · 토스페이먼츠 결제
- [x] **6단계** — 주문내역 · 마이페이지 · 관리자 상품/주문 관리
- [x] **7단계** — 디자인 마감 · README · git 커밋 (GitHub·Vercel 연결은 사람이 할 일)

### git 커밋

- 커밋 메시지는 **한국어**로, `1단계: 프로젝트 뼈대와 회원가입/로그인` 처럼 단계와 내용을 적습니다.
- 커밋 · 푸시는 **사용자가 요청할 때만** 합니다.
